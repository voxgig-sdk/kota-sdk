# Entity: create_session_token

main: kit: entity: create_session_token: {

  alias: field: {}

  fields: [
    {
      format: "date-time"
      name: "expiry"
      req: true
      type: "`$STRING`"
    }
    {
      name: "token"
      req: true
      type: "`$STRING`"
    }
  ]

  name: "create_session_token"
  op: {
    create: {
      name: "create"
      points: [
        {
          method: "POST"
          orig: "/embed/sessions"
          segments: [
            {
              lit: "embed"
            }
            {
              lit: "sessions"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
