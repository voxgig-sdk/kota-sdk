<?php
declare(strict_types=1);

// Kota SDK utility: feature_hook

class KotaFeatureHook
{
    public static function call(KotaContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
